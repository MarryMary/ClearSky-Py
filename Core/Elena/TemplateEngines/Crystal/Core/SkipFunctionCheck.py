class SkipFunctionCheck:
    def checker(self, function):
        flag = False
        registered = self.registered_function()
        for(k, v in registered.items()):
            pass
        return flag

    def registered_function(self):
        with open("")